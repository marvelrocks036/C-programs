#include <iostream>
using namespace std;
int main() 
{
    float a, b, temp;
    cout << "Enter any two numbers \n";
    cin >> a >> b;
    temp = a * b;
    cout << "The product of the two numbers is \n" << temp;
    return 0;
}